# PostgreSQL — use postgresql+psycopg:// (not postgresql://) for app and Alembic
# Local (docker/docker-compose.yml):
DATABASE_URL=postgresql+psycopg://ferry:ferry@localhost:5432/ferry_wait
# Prod (Neon): same prefix; store in GitHub Secrets as DATABASE_URL

# SendGrid alerts (phase 0.8)
SENDGRID_API_KEY=
ALERT_EMAIL_FROM=
ALERT_EMAIL_PARSE=
ALERT_EMAIL_FRESHNESS=
