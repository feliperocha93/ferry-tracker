# Copy to .env.terraform (gitignored) and fill in. Same shell as: make tf-init / make tf-plan
#
#   cp .env.terraform.example .env.terraform

TF_CLOUD_ORGANIZATION=ferry-wait
# User API token from https://app.terraform.io/app/settings/tokens
TF_TOKEN_app_terraform_io=
NEON_API_KEY=
